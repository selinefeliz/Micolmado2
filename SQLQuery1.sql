Use MiColmado
CREATE TABLE users (
    userID   INT PRIMARY KEY IDENTITY,
    userName VARCHAR (50),
    upass    VARCHAR (10),
    uName    VARCHAR (50),
    uPhone   VARCHAR (20),
    uImage image
    )
select * from users